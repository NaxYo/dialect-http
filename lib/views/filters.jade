- var filters = dialect.config('filters');
div
  h1 Translation filters

if filters
  div.filters-buttons
    -each filter in filters
      a.filter-button(href= '/set-filter/' + filter.url)= filter.name
  div.filter-unset
      a.unset-button(href= '/unset-filter') Unset filter

else
  span No filter yet ...
